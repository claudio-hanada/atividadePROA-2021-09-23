inicio
	variavel real valor
	variavel real total
	contador <- 1
	
	enquanto (contador < 11) faca
		ler(valor)
		total <- total + valor
		contador <- contador +1
	fimEnquanto
	
	escreva("Soma: ", total)
fim
